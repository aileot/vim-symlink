name: CI
on:
  push:
    branches:
      - main
  pull_request:
jobs:
  ci:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: install
        run: make install
      - name: lint
        run: make lint
      - name: test
        run: make test
